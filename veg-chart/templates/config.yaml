apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  MONGO_URI: {{ .Values.configmap.data.MONGO_URI }}
  MONGO_INITDB_DATABASE: {{ .Values.configmap.data.MONGO_INITDB_DATABASE }}
