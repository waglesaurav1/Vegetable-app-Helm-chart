apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-headless
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.headless.selector.app  }}
  ports:
    - protocol: TCP
      port: {{ .Values.headless.port }}
      targetPort: {{ .Values.headless.port }}
  clusterIP: {{ .Values.headless.clusterIP }}
